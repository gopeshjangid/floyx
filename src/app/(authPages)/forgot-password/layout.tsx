import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Floyx Reset Password',
  description: 'Floyx Reset Password',
};

export default function Page({ children }: { children: React.ReactNode }) {
  return children;
}