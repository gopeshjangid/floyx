import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Floyx Social Login',
  description: 'Floyx Social Login',
};

export default function Page({ children }: { children: React.ReactNode }) {
  return children;
}
